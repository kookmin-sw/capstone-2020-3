import { ChangeFahrenheitPipe } from './change-fahrenheit.pipe';

describe('ChangeFahrenheitPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeFahrenheitPipe();
    expect(pipe).toBeTruthy();
  });
});
